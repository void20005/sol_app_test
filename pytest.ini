[pytest]
#testpaths = tests
#addopts = --alluredir=allure-results --reruns 1 --reruns-delay 2

markers =
    ui: marks UI tests (e.g., frontend tests)
    api: marks API tests
    integration: marks integration tests
    smoke: smoke tests
    regression: regression tests
    e2e: End to end tests